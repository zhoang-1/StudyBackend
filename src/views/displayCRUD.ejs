<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Display CRUD</title>
</head>
<style>
    table,
    th,
    td {
        border: 1px solid black;
        border-collapse: collapse;
        font-size: 18px;
    }

    .btn-edit {
        color: black;
        display: block;
        float: left;
        border: none;
        text-decoration: none;
        background: rgb(154, 245, 69);
        outline: none;
        height: 25px;
        margin: 5px;
        width: 70px;
        cursor: pointer;
        text-align: center;

    }

    .btn-delete {
        color: black;
        display: block;
        float: left;
        border: none;
        text-decoration: none;
        background: rgb(200, 17, 108);
        outline: none;
        height: 25px;
        margin: 5px;
        width: 70px;
        cursor: pointer;
        text-align: center;
    }
</style>

<body>
    <table style="width:100%">
        <tr>
            <th>Email</th>
            <th>Firstname</th>
            <th>Lastname</th>
            <th>Address</th>
            <th>PhoneNumber</th>
            <th>gender</th>
            <th>roleId</th>
            <th>Action</th>
        </tr>

        <% for(let i=0; i < dataTable.length; i++) { %>
            <tr>
                <td>
                    <%= dataTable[i].email %>
                </td>
                <td>
                    <%= dataTable[i].firstName %>
                </td>
                <td>
                    <%= dataTable[i].lastName %>
                </td>
                <td>
                    <%= dataTable[i].address %>
                </td>
                <td>
                    <%= dataTable[i].phoneNumber %>
                </td>
                <td>
                    <%= dataTable[i].gender %>
                </td>
                <td>
                    <%= dataTable[i].roleId %>
                </td>
                <td>
                    <a href="/edit-crud?id=<%= dataTable[i].id %>" class="btn-edit">Edit</a>
                    <a href="/delete-crud?id=<%= dataTable[i].id %>" class="btn-delete">Delete</a>
                </td>
            </tr>
            <% } %>
    </table>
</body>

</html>